# ============================================================================
# PREPWISE FRONTEND ENVIRONMENT CONFIGURATION TEMPLATE
# ============================================================================
# This is an example configuration file for the Next.js frontend.
# Copy this to .env.local and fill in your actual values.
#
# Setup Instructions:
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in all values
# 3. Ensure .env.local is in .gitignore
#
# CRITICAL SECURITY WARNING:
# All variables prefixed with NEXT_PUBLIC_ are exposed to the browser!
# These values are embedded in the JavaScript bundle and visible to users.
# NEVER put sensitive backend API keys in NEXT_PUBLIC_ variables.
# ============================================================================

# ============================================================================
# BACKEND API CONFIGURATION
# ============================================================================
# Public URL to your backend API
# Development:
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Production (update to your deployed backend URL):
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api/v1
# NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com

# ============================================================================
# VAPI VOICE CONFIGURATION (Optional - for voice interview features)
# ============================================================================
# Get your keys from: https://dashboard.vapi.ai
#
# SECURITY NOTES:
# - These keys are exposed client-side (in browser)
# - Use VAPI's security features:
#   1. Enable domain/origin restrictions in VAPI dashboard
#   2. Set allowed origins to your deployment domain only
#   3. Monitor usage and set up alerts
#   4. Rotate keys if exposed in public repo
#
NEXT_PUBLIC_VAPI_API_KEY=your-vapi-api-key-here
NEXT_PUBLIC_VAPI_ASSISTANT_ID=your-vapi-assistant-id-here

# ============================================================================
# ANALYTICS & MONITORING (Optional)
# ============================================================================
# Vercel Analytics (if using Vercel deployment)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=your-vercel-analytics-id

# Google Analytics (optional)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================================================
# SECURITY BEST PRACTICES
# ============================================================================
# DO:
# ✓ Use NEXT_PUBLIC_ only for truly public configuration
# ✓ Keep all sensitive keys on the backend
# ✓ Use server-side API routes for authenticated operations
# ✓ Enable domain restrictions on client-side API keys
# ✓ Monitor usage of client-exposed keys
#
# DON'T:
# ✗ Never put backend database credentials here
# ✗ Never put OpenAI/Stripe secret keys here
# ✗ Never put JWT secrets here
# ✗ Never commit .env.local to version control
# ============================================================================
