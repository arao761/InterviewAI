# FastAPI Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.44  # Updated for Python 3.13 compatibility
alembic==1.17.2  # Updated for SQLAlchemy 2.0.44
# psycopg2-binary==2.9.9  # PostgreSQL - not needed for SQLite

# Authentication (optional for MVP)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Environment & Configuration
python-dotenv==1.0.0
pydantic-settings==2.1.0
email-validator>=2.0.0  # Required for pydantic EmailStr validation

# File Handling
aiofiles==23.2.1
PyPDF2==3.0.1
python-docx==1.1.0

# API Documentation
# Use existing pydantic version (2.12.4 already installed)

# Task Queue (optional)
celery==5.3.6
redis==5.0.1

# Logging
python-json-logger==2.0.7

# HTTP Client for external APIs
httpx==0.26.0

# Payment Processing
stripe==7.8.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3

# Development
black==24.1.1
flake8==7.0.0

# AI/NLP Dependencies - NOTE: These are managed by prepwise-ai
# The versions below are listed for reference only
# Actual versions will come from prepwise-ai installation
# openai==1.12.0
# anthropic==0.18.1
# langchain==0.1.9
# langchain-openai==0.0.6
# spacy==3.7.4

# PrepWise AI Integration - Install as editable package
# This will install all the AI/NLP dependencies automatically
# NOTE: prepwise-ai is installed via render.yaml build command
# For local development: cd backend && pip install -e ../prepwise-ai

# Use pydantic from prepwise-ai (2.12.4)
